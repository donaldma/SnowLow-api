{
  "name": "snowlow-api",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run lint && rimraf build && tsc && sls offline start --skipCacheInvalidation --port 3001",
    "deploy:dev": "npm i && npm run lint && rimraf build && tsc && sls deploy -s dev",
    "deploy": "npm i && npm run lint && rimraf build && tsc && sls deploy -s prod",
    "lint": "tslint \"src/**/*.ts\" ",
    "migrate": "sls dynamodb migrate"
  },
  "keywords": [],
  "author": "Donald Ma",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-sdk": "^2.7.0",
    "@types/dotenv": "^4.0.2",
    "@types/node": "^9.4.0",
    "@types/uuid": "^3.4.3",
    "rimraf": "^2.6.2",
    "serverless-offline": "^3.16.0",
    "tslint": "^5.9.1"
  },
  "dependencies": {
    "@types/cheerio": "^0.22.7",
    "@types/lodash": "^4.14.102",
    "@types/moment": "^2.13.0",
    "@types/request": "^2.47.0",
    "@types/validator": "^6.3.0",
    "aws-sdk": "^2.188.0",
    "bcrypt": "^1.0.3",
    "cheerio": "^1.0.0-rc.2",
    "dotenv": "^5.0.0",
    "http-errors": "^1.6.2",
    "lodash": "^4.17.5",
    "moment": "^2.20.1",
    "moment-timezone": "^0.5.14",
    "node": "^9.5.0",
    "request": "^2.83.0",
    "request-promise": "^4.2.2",
    "serverless-dynamodb-local": "^0.2.27",
    "twilio": "^3.13.0",
    "typescript": "^2.7.1",
    "uuid": "^3.2.1",
    "validator": "^9.4.0"
  }
}
